import GameScene from './game_scene';

class MainScene extends GameScene {
  constructor (ctx) {
    super(ctx);
  }
 
  init () {
  }

  handleMouseDown (e) {
  }

  handleKeyPress (e) {
  }
}

export default MainScene;
